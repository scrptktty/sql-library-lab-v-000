CREATE TABLE series (
  id INTEGER PRIMARY KEY,
    name TEXT,
    age INTEGER
);

CREATE TABLE subgenres (
  id INTEGER PRIMARY KEY,
    name TEXT,
    age INTEGER
);

CREATE TABLE authors (
  id INTEGER PRIMARY KEY,
    name TEXT,
    age INTEGER
);
